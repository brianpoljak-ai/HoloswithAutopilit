```markdown
# HoloswithAutopilit
```

Deployment (live backend)
- Deployed backend URL: `https://holos-autopilot-lvfxjikhp-brianpoljak-ais-projects.vercel.app`
- Marketing signup script (`marketing/signup.js`) now defaults to this backend; override by setting `window.HOLOS_BACKEND_URL` on the marketing pages for local/dev testing.
# HoloswithAutopilit